# Startup recovery

Package: `cybertron_startup_recovery.zip`

Recovery package that restored known-working files after a failed stability build and added startup logging so crashes would open a readable log instead of silently closing.
